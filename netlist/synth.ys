# 读取设计文件
read_verilog alu.v

# 检查语法
hierarchy -check

# 选择顶层模块
proc; opt; fsm; opt; memory; opt

# 映射到通用工艺库
techmap; opt

# 使用ABC进行逻辑优化
abc -g AND,XOR,OR

# 生成网表
write_verilog alu_synth.v

# 生成统计信息
stat